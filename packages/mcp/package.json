{
  "name": "@md/mcp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": { "vitest": "^2.1.0" }
}
