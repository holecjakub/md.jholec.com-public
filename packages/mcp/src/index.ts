// WIP PLACEHOLDER — not implemented yet.
//
// This package is a deliberate phase-2 stub (see spec §10). It will host an MCP
// server that wraps @md/core, but currently exports nothing. It exists so the
// package boundary is reserved and so `pnpm -r` fan-outs (typecheck/lint/test)
// have a target to succeed against. Do not delete it (roadmap phase-2); replace
// the body below when implementation starts.
export {};
