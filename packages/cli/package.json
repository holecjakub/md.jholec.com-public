{
  "name": "@md/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": { "md": "dist/index.mjs" },
  "files": ["dist"],
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "pnpm exec tsc --noEmit",
    "lint": "pnpm exec eslint src",
    "test": "pnpm exec vitest run"
  },
  "dependencies": { "@md/core": "workspace:*", "commander": "^12.1.0" },
  "devDependencies": { "esbuild": "^0.27.0", "vitest": "^3.2.4" }
}
