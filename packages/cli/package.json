{
  "name": "@md/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": { "md": "dist/index.mjs" },
  "files": ["dist"],
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "@md/core": "workspace:*", "commander": "^12.1.0" },
  "devDependencies": { "esbuild": "^0.27.0", "vitest": "^2.1.0" }
}
